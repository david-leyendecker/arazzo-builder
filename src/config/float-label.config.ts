/**
 * Global FloatLabel configuration
 * Change the variant here to apply it across the entire application
 * Options: 'over' | 'on' | 'in'
 */
export const floatLabelConfig = {
  variant: 'on' as 'on' | 'over' | 'in'
}
